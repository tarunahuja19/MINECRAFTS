# ---- Dependencies (regenerable from lockfiles) ----
node_modules/
.venv/
.venv-train/
__pycache__/
*.pyc

# ---- Secrets ----
.env
*.env
!.env.example

# ---- Generated simulation artefacts ----
out/
scratch/
.pytest_cache/
dist/
*.tsbuildinfo

# ---- simulation/ : vendored as source (was a nested repo). Keep code, drop the rest ----
simulation/out/
simulation/.venv/
simulation/frontend/node_modules/
simulation/frontend/dist/
simulation/ogs_pyvista_mine/
simulation/opensees_sim/

# ---- ml_dataset_generator/ : vendored as source (was a nested repo) ----
# Large generated dataset + training artefacts + envs (regenerable; multi-GB)
ml_dataset_generator/dataset/
ml_dataset_generator/training/cache/
ml_dataset_generator/training/checkpoints/
ml_dataset_generator/.venv/
ml_dataset_generator/.venv-train/
*.npz

# ---- OS noise ----
.DS_Store

# ---- Electron / launcher runtime logs ----
.electron.log
.stack.log
.pyvista.log
.broker.log

# ---- Session transcript dump, not project source ----
checkout.md
