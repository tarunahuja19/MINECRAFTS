# ---- Dependencies (regenerable from lockfiles) ----
node_modules/
.venv/
.venv-train/
__pycache__/
*.pyc

# ---- Nested repo: simulation/ keeps its own git history and branches ----
# Tracked independently; do not vendor it into the root repo.
simulation/

# ---- Secrets ----
.env
*.env
!.env.example

# ---- Generated simulation artefacts ----
out/
scratch/
.pytest_cache/

# ---- Large generated ML dataset (regenerable; ~2 GB) ----
ml_dataset_generator/dataset/
ml_dataset_generator/training/
ml_dataset_generator/.venv-train/

# ---- OS noise ----
.DS_Store

# ---- Nested repo: ml_dataset_generator/ keeps its own git history ----
# Tracked independently at github.com/agarwaladarshcoding-maker/similation-data-making
ml_dataset_generator/

# Electron runtime log written by scripts/start_all.sh
.electron.log

# Runtime logs written by the launcher
.stack.log
.pyvista.log
.broker.log

# Session transcript dump, not project source
checkout.md
