# Project update — 2026-09-15 (Tuesday)

## Summary of earlier days
- **2026-09-13** ([file](2026-09-13.md)): repo + vault audit, 3-day sprint plan, DEC-1…DEC-4. 15 tests.
- **2026-09-14** ([file](2026-09-14.md)): Step 0 review; plan reset (Jobs 1–3); Claude built M1–M5; stress test + NaN fix; W1–W4 fixed (peak −5% vs field, 9% pinned); real-anchored dataset; CI; status check (v1 left: T1–T4 + demo). 129 tests.

## Summary of this day
- **Session 10:** one command `./run-all.sh` rebuilds everything from scratch on the laptop (121 s, output byte-identical to committed v2); D1–D7 ratified (all yes); DEC-16 no database in Part 1; plans repointed from erased `out/v1-*` to `out/v2-690d`. 129 tests.
- **Session 11:** visualization not built yet (T4 first); web packages already installed; T4 prompt sharpened for v2 data.
- **Session 12:** Window 1 rebuilt on real SRTM terrain (old sandbox style: height + sink ramps, hillshade, pixel contours, strata block, X-ray, section S); node planner v2 over the terrain (Poisson-disc by strain band, slope exclusion, fan-out anchors, line-of-sight gateway). 138 tests.
- **Session 13:** committed sessions 10–12 (`685bbd4`); `run-all.sh` → `run-simulation.sh`; fixed silent stop at step 6 when `run_summary.json` really changes; full run from scratch OK (170 s); plain-words recap; cleanup deletions blocked by the permission check, handed to Adarsh. 138 tests.
- **Session 14:** branch `fix-the-system`; checked view, planner, terrain, data (network in a 655 m corner the face leaves by ~day 150, 34 anchor arcs = the wires, nodes drawn ×1.8, only 25 nodes in the 690-day run); plan F0 + prompts F1–F5 in `steps/`; no code written. 138 tests.
- **Session 15:** checked F1 (Antigravity): wires gone, click links + blocked label OK; 7 fixes (gateway 2 legs, size slider stretches rod/wire, wire LOD from sector centre, copied fallback numbers, duplicate colours, test reads stale scene.json, walkthrough numbers wrong); F2 not started. Renderer 10 · mine-sim 138.

---

## Session 10 — 2026-09-15 (00:00–01:00) — One-command rebuild, D1–D7, no database

**Actor:** Claude Code · **Supervisor:** Adarsh

### Adarsh's direction
1. One command that runs the entire system from scratch, erasing the generated data first.
2. No Postgres / DB build-up: the backend and frontend (same two people) already work; data is read from files on Adarsh's laptop, also while the run is going.
3. "Yes for all" → D1–D7.

### Done
- **`run-all.sh`** (repo root, new). `./run-all.sh` = 690 days; `./run-all.sh N` = N-day run into `out/v2-Nd` (keeps other runs and `handoff/`). Steps: erase → refit from `data/real` → pytest → simulate with a live `day N / 690` counter read from the growing `nodes.csv` → real-anchored dataset → package `handoff/v2-sim-sample` → verify (row count = scouts × epochs, 0 duplicate `(node_id, epoch)`). Never erases `data/real`, fixtures, config, `handoff/v1-sample`.
- **Verified:** full run 121 s, 129 passed, 414,000 rows, 0 duplicates, delivery 0.9996, peak 1,204 mm. Refit params, `nodes.csv` (sha256 `ee340beb…`) and real-anchored data **byte-identical** to the committed v2. Only gzip header + `wall_time_s` differ → restored with `git checkout`. 2-day quick run: keeps `out/v2-690d`.
- **Decisions (vault §11):** D1–D7 yes; DEC-14, DEC-15 ratified; WP9 binding; **DEC-16 no database in Part 1**, laptop files are the data source. Changelog `2026-09-15-claude-code-run-all-command-d1-d7`.
- `files/MY-STEPS.md` §A marked answered. `work-with-system/2026-09-15-tue.md` new **S1c** (how to run it, what to see).
- **Path fix:** the run erased `out/v1-30d`, `out/v1-690d` (superseded). T4, T7, Wed lab prompts and Wed hands-on steps now point at `out/v2-690d` (`BUILD-PLAN.md` M5 history text left as is).

### Notes
- Files are written with normal buffering: a reader during a run can see a half-written last line; skip it.
- `run-all.sh` runs no stress test (CI does) and no vault check.
- Wednesday prompt W1 reference values ("≈ 524 mm", "≈ 147 mm") were computed on 14 Sep before the session 08 refit; recheck them before W1 is pasted.

### Open
- [ ] Adarsh: send teammate messages (paths `handoff/v2-sim-sample/`, `handoff/v2-real-anchored/`, or `mine-sim/out/v2-690d/` on the laptop); GitHub invites; `pip install httpx websockets`.
- [ ] 09:15: T1–T4 into AG-1…AG-4.
- [ ] Commit this session (not committed yet).
- [ ] Claude: recheck W1 reference values before Wednesday.

### Integrity at end
Vault 0 broken / 0 orphans / 0 frontmatter · mine-sim **129 passed**.

---

## Session 11 — 2026-09-15 (early morning) — Visualization status, T4 prompt ready

**Actor:** Claude Code · **Supervisor:** Adarsh

### Adarsh's direction
Where is visualization? Install websockets. Prepare the Antigravity prompts, one at a time. Teammate messages go later.

### Done
- **Visualization status:** nothing built yet. Window 1 (3D terrain, T4) is v1; Window 2 scenario page (W4) and forecast view (W8) are Wednesday.
- **Install:** nothing needed; `fastapi 0.141.1`, `httpx 0.28.1`, `websockets 17.1`, `uvicorn 0.52.4` already in pinn-sandbox.
- **T4 prompt sharpened** in `files/BUILD-PLAN.md` against the real v2 data: exact file list, npz/jsonl/csv formats, single jsonl pass, `renderer/config.yaml` for display values, face x from config (no physics import), test on a 2-day run vs `replay_terrain`, served with `python3 -m http.server`.
- **Prompt order (one window, one at a time):** T4 renderer → T1 gates + G14 → T2 WebSocket → T3 cmp check.

### Open
- [ ] Adarsh: AG-4 connect block → T4 → paste report to Claude for review.
- [ ] Commit sessions 10–11 (still uncommitted).

---

## Session 12 — 2026-09-15 (afternoon) — Real-terrain 3D renderer, node planner v2

**Actor:** Claude Code (built at Adarsh's explicit request) · **Supervisor:** Adarsh

### Adarsh's direction
The simulation works on the mine data but the design and terrain are "very very bad". Study `sih26/simulation` (terrain, design, 3D; ignore ML) and build the simulation on our data. Then use the placement algorithm to plan nodes over the terrain, reusing old code as a base.

### What the old repo taught (read, not copied blindly)
- `frontend/src/components/TerrainMesh.tsx`, `utils/hypsometry.ts`: height ramp by equal-area rank + Horn hillshade + rock tint; separate blue→red **sink** ramp (never through green) with excavation shadow and contours; datum-relative real DEM. Ported to `renderer/js/ramps.js`, `terrain.js`.
- `NodeMarkers.tsx`: tier by shape, state by beacon colour, commissioned parent/backup links. Ported to `network.js`.
- `ml_dataset_generator/minegen/layout.py`, `MESH_UPGRADE_BRIEF.md`: weighted dart-throwing, anchors derived from fan-out, capacitated greedy parents. Base of `minesim/placement.py`.
- **Bug found in the old sandbox:** `sandbox/dem.py` assumed the site sits at the DEM tile centre. The Adriyala lat/lon is at pixel (46, 4) of that tile, ~2.2 km away, so its terrain was misplaced. New DEM is fetched and georeferenced per sample (`scripts/fetch_dem.py`).

### Done
- **Real terrain:** `mine-sim/data/real/adriyala_lw1_dem.npz` (AWS Terrain Tiles, SRTM-derived, z13, 15 m, 71–212 m AMSL). It shows the real Ramagundam landscape: an open-cast pit (~70 m) and overburden dumps that the panel crosses.
- **Node planner v2** (`python -m minesim.placement` → `mine-sim/out/plan/node_plan.json`): survey sector (line S ± travelling window). 237 scouts (34 1A · 164 1B · 39 1C), 34 anchors, 1 gateway, ₹5.71 lakh. 17 monuments on line S placed first. Min spacing 20.0 m, NN CV 0.30 (not a grid), max 8 children, 191 steep moving cells skipped, worst link margin 42.9 dB, 220/237 scout and 28/34 backbone links terrain-clear.
- **Window 1 renderer** (`./run-all.sh view`): real terrain block with strata walls; subsidence from the world grid every 10 days (interpolated); per-pixel depth contours; face curtain; panel, survey line and sector draped; underground X-ray (seam, goaf growing with the face, shearer, angle of draw); **Section S** cross-section (day-0 vs today); planned network with tier shapes, sink-coloured beacons, red terrain-blocked links; v1 sensor-run nodes by provenance; inspector; 6 camera presets.
- Exporter adds `terrain`, `geometry`, `plan`, `plan_dz_mm` (existing keys untouched). `run-all.sh` plans before export. Screenshots checked in headless Chrome for every camera.
- Test found a real planner bug (tier decided at the raster cell, node jittered inside it): the final tier now comes from the node's own peak strain.

### Notes / limits
- Site lat/lon and advance bearing are **OPEN — VERIFY** (old sandbox value). A wrong site moves the terrain under the panel.
- The panel is subcritical, so no ground in the sector is quiet: anchors sit on moving ground (anchor #2: −1,080 mm, 5.9 mm/m). Worth a design discussion (anchors outside the trough?).
- Placement v2 is **not** in the sensor run yet; `nodes.csv` still has the 25 v1 cross nodes (shown as the "Sensor-run nodes" layer).
- Terrain is context only: it never feeds S(x,y,t) (Invariant 4) and never writes Z (Invariant 1). three.js pinned at r147 (last release with `three.min.js` + `examples/js` OrbitControls, which the page test requires).
- Planner tests add ~33 s to pytest (75 s total).

### Open
- [ ] Adarsh: review the new Window 1 (`./run-all.sh view`). Decide whether it replaces the Antigravity T4 fix prompt.
- [ ] Adarsh: promote placement v2 into `size_network` / the sensor run? (~270 nodes → larger `nodes.csv`)
- [ ] Real panel coordinates and bearing from the SCCL mine plan / JMMF figure.
- [ ] Commit sessions 10–12 (still uncommitted).

### Integrity at end
Vault 0 broken / 0 orphans / 0 frontmatter · mine-sim **138 passed** · renderer tests **8 passed**.

---

## Session 13 — 2026-09-15 (15:00) — Commit, one "run simulation" command, cleanup plan

**Actor:** Claude Code · **Supervisor:** Adarsh

### Adarsh's direction
Remove every unneeded file or folder, write in simple words what he asked and what Claude did, commit, and make one command "run simulation" that runs everything.

### Done
- Start checks: vault PERFECT (0 broken / 0 orphans / 0 frontmatter), mine-sim **138 passed**.
- Committed sessions 10–12 as `685bbd4` (not pushed).
- `run-all.sh` → **`run-simulation.sh`** (`git mv`). Live references updated: `steps/*`, `files/BUILD-PLAN.md`, `files/REAL-MINE-DATA-PLAN.md`, vault `decisions.md`. Older session logs and changelogs keep the old name (history).
- **Bug fixed in the script:** `keep_if_same` returned non-zero when a file really differed, and `set -e` then stopped the run silently at step 6. Trigger: session 12 added planner keys to `assumptions.yaml`, which changes `config_hash` in `run_summary.json` (sensor data byte-identical). The helper now always returns 0.
- Full run `./run-simulation.sh --no-view`: exit 0, 170 s, 138 passed, 414,000 rows (expected 414,000), 0 duplicates, delivery 0.999609, peak 1,204 mm, plan 237 scouts / 34 anchors / 1 gateway (INR 571,400), scene 5.72 MB. `handoff/v2-sim-sample/run_summary.json` updated (new config hash only).
- `walkthroughs/step-09-wp8-renderer/PLAIN-WORDS.md`: what Adarsh asked, what Claude understood, what Claude did, sessions 10–13.

### Not done
- **Deletions blocked** by the Claude Code auto-mode permission check (both `rm` and `git rm`). Proposed and handed to Adarsh: `work-with-tools/`, `work-with-system/` (replaced by `steps/`), `handoff/v1-sample/` (superseded by v2), Python caches. After they are removed, pointers in `CLAUDE.md`, `files/MY-STEPS.md`, `files/README-start-here.md`, `files/BUILD-PLAN.md` line 6 and `files/TEAM-BRIEF-V1.md` still need updating.

### Open
- [ ] Adarsh: run the delete commands (or allow them), then Claude fixes the pointers.
- [ ] Adarsh: push to GitHub when ready (CI runs on push).
- [ ] Carried over: T4 decision, placement v2 into the sensor run, real panel coordinates.

### Integrity at end
Vault 0 broken / 0 orphans / 0 frontmatter · mine-sim **138 passed**.


---

## Session 14 — 2026-09-15 (17:00) — Branch `fix-the-system`: checks + plan F0–F5

**Actor:** Claude Code (planner + checker, no code written) · **Supervisor:** Adarsh

### Adarsh's direction
New branch "fix the system". Remove the wires in the view. Check the placement algorithm (network squeezed into a small region). True-scale terrain with + / − zoom to fixed levels, bigger terrain. Distinct designs for the node types. Check the terrain data. More data: longer than a year, slow play (about a day at 1×) with 10× / 20× / 40× and faster. Write the plan and the Antigravity prompts; Claude plans and checks and doesn't write code.

### Done
- Start checks: vault PERFECT (0 broken / 0 orphans / 0 frontmatter), mine-sim **138 passed**.
- `git checkout -b fix-the-system` (the staged cleanup deletions from session 13 came along, still uncommitted).
- Measured the current state: screenshot at day 345 (34 anchor→gateway arcs + 237 scout lines on by default). Plan sector x −69…585 m of 2,500 m, so all 237 scouts sit in 655 × 410 m and the face (1,380 m on day 345) is far outside. `_sector_bounds` is static although the yaml says the sector travels. One 20 m spacing for all tiers (1B 69 %). Anchor min spacing 31.6 m. 6/34 anchor and 17/237 scout links blocked. Nodes drawn ×1.8 (pads ≈5 m, masts up to 54 m). DEM 5.7 × 3.45 km real SRTM, renderer draws 800 m around the grid only. The run is 690 d hourly but has 25 v1 nodes; the view samples every 10 days.
- Wrote `steps/F0-fix-the-system-plan.md` (findings, decisions DF1–DF4 with defaults, order) and prompts `F1` (no wires + node designs), `F2` (true scale, fixed zoom levels, LOD icons, DEM margin 3 km), `F3` (placement v3: full footprint, spacing by tier, spread anchors, link-aware siting), `F4` (plan network in the sensor run via `size_network(cfg)` unchanged signature; daily int16 binary frames), `F5` (sim clock, 1× = 360 sim s per real s, speeds 1…10000×). `steps/STATUS.md` updated.

### Decisions pending (defaults proposed)
- DF1 1× = 6 sim min per real s (a year ≈ 24 h). DF2 keep 690 days. DF3 planned network becomes the sensor-run network (~5 M rows). DF4 refetch DEM with 3 km margin.

### Open
- [ ] Adarsh: confirm DF1–DF4, then F1 into Antigravity (F3 in parallel in a second chat).
- [ ] Carried over: commit/push the session-13 deletions; site lat/lon + bearing OPEN — VERIFY.

### Integrity at end
Vault 0 broken / 0 orphans / 0 frontmatter · mine-sim **138 passed** (no code changed this session).

---

## Session 15 — 2026-09-15 — Check F1, F2 status

**Actor:** Claude Code (checker, no code written) · **Supervisor:** Adarsh

### Done
- Start checks: vault PERFECT, mine-sim **138 passed**, renderer **10 passed** (needs `PYTHONPATH=.` from repo root).
- F1 written by Antigravity (network.js rewrite, config node_models, export passes A15 heights + A8/A9 baselines, 2 new tests, 8 screenshots). Scope OK: only listed files changed.
- Verified: no links at default, "All radio links (debug)" + sector unchecked, X-ray gates rays/posts, thin face band, scout parent solid / backup dashed, anchor children + straight gateway line, red "terrain blocks radio" label, SVG legend, inspector text, pick 6 m ≤ 10 m, slider 1–10 default 1.
- 7 fixes written into `steps/F1` section 4: gateway legs ignore z (only 2 legs visible); slider scales rod/extensometer baselines and pick radius; 1C wire LOD measured from sector centre; fallback numbers copy assumptions (2/2/10, 10, 30) and config; TIER_SVG duplicates colours; antenna/baseline test reads on-disk scene.json; walkthrough dimensions and the scout→gateway claim don't match the code.
- F2: not started (no DEM or zoom changes). Prompt still valid. Note: it raises `dem_margin_m` 1300 → 3000 and regenerates the DEM, which changes placement input, so F3 results must be regenerated after F2.
- `steps/STATUS.md`: F1 Written ✅ / Checked ✅ 7 fixes.

### Open
- [ ] Adarsh: paste F1 fix prompt → `Check step F1`; then F2.

### Integrity at end
Vault 0 broken / 0 orphans / 0 frontmatter · mine-sim **138 passed** · renderer **10 passed**.
