# 2026-09-24

## Summary of earlier days

18 Sep (session 28): integration with the R4 frontend/backend repo designed, no feature code — plan only. R4 is a complete older system (Express :8080, Postgres :5432, MQTT :1883, Electron + Leaflet :8085); our simulator replaces its Box-2 Python engine. Nine silent breakages measured; five sittings A–E planned. vault PERFECT, mine-sim 198 passed / 1 skipped, scenario-lab 125 passed.

## Summary of this day

- **Session 29 (in progress)** — Adarsh locked: Adriyala LW1 only (no second mine), new DB = local docker, scope = as much as we can. Two repos mapped (`sih-26-Micecraft` = R4 app, `simulation-terrian` = simulator); VSCode workspace fix given. Electron state read from its files (no Simulation button yet — E1 is real work). Origin match verified 18.6435/79.5725 in both configs. Shell tool down → integrity checks, P3 smoke, and Electron run all PENDING.

---

## Session 29 — same mine, repo link, Electron first look (code reads only)

**Decisions (Adarsh):** location stays Adriyala LW1 — no second mine, no new `config/mines/*.yaml`. New hosted DB = local docker tonight, cloud later. Scope = do as much as possible (D1+D2+E1 must, D3/E2 stretch). Project-updates file to be kept current on the go.

**Repo connection (verified by reading `.git/config` files):** two separate repos, nested working trees.
- `/Users/adarshagarwala/Documents/sih26` → `sih-26-Micecraft.git` (R4 team app: backend, Electron, old simulation).
- `/Users/adarshagarwala/Documents/sih26/sih-26-finale` → `simulation-terrian.git`, branch `feat/p3-lab-server` (this work).
- Parent `.gitignore` does NOT mention `sih-26-finale`. The two repos join at runtime (ports/APIs), not via git.
- "Can't see the whole thing": VSCode is opened at `sih-26-finale`, so the parent (Electron etc.) is invisible. Fix: File → Open Folder `/Users/adarshagarwala/Documents/sih26` (or Add Folder to Workspace).

**Electron current state (from reading its files — main.js, preload.js, serve.js, renderer/index.html, app.js, panel-grid.js, area-select-3d.js, map-view.js):**
- HAS: MAP/NODES/ALARMS/HISTORY/SYSTEM/INFO tabs; Leaflet 2D (AERIAL/TOPO, GRID/LABELS/SURFACE); 8×8 sectors A1–H8 over a 600 m window; cursor marquee → 3D workstation pane/popout (`window:open-3d` → `3d-window.html?sector=`); alarm banner + replay + node tables + MQTT log + export report + INFO manual. Tile proxy :8085 starts in-process; fixture mode when backend/MQTT absent.
- NOT: no popup with 3D View / Simulation choice; no Simulation button; no 10-slice seg concept (theirs: 8×8 sectors + 6×6 zones); nothing points at :8010. 3D relief still flat (known issue, handed off — our renderer is the working 3D).
- Marquee path today uses Leaflet-native `mouseEventToLatLng`/`L.latLngBounds`; grid metres→lat/lon via `mapView.xyToLatLon` (same flat-earth form as our geo). F3's missing-function shape has changed since session 28 — needs a runtime re-check, not assumed.

**Maths/connection (verified):** origin 18.6435 N / 79.5725 E identical in `mine-sim/config/mines/adriyala_lw1.yaml` and `dashboard_electron/.../map-view.js`. Lab server exists on branch (`scenario-lab/lab/server.py`, stdlib, port 8010 in `config/lab.yaml`, `/health`+`/control` Box-2 drop-in, writes fenced to `store/`).

**PENDING (shell tool `muse-spark` classifier down — Bash + Agent refused all session):** `sync_vault.py --check`, mine-sim + scenario-lab pytest counts, P3 smoke (`2252` + negative sinking), local-docker Postgres up, D1/D2 prompts, Electron live run + screenshot. Nothing merged; no code written this session.

### Open — needs Adarsh or shell recovery
- Run Electron yourself to see it: `cd /Users/adarshagarwala/Documents/sih26/dashboard_electron && npm start` (needs `npm install` once; works without backend/MQTT in fixture mode).
- P3 merge stays gated on the smoke test (2252 + negative) — I run it the moment the shell recovers.
- D1 (schema) + D2 (loader) prompts go to Antigravity once docker Postgres is up.
