# Project Updates

**One file per date.** Each file starts with a summary of earlier days and a summary of that day's sessions, then the full session logs in order. Newest date at the top. Claude reads the newest file at the start of every session (see `CLAUDE.md`).

| Date | File | Sessions | Summary |
|---|---|---|---|
| 2026-09-24 | [2026-09-24.md](2026-09-24.md) | 29 | **Session 29 (in progress):** Adriyala-only locked, new DB = local docker, scope = as much as possible. Two repos mapped (parent = `sih-26-Micecraft`, finale = `simulation-terrian`); VSCode workspace fix given. Electron state read from files (no Simulation button yet — E1 is real work); origin 18.6435/79.5725 matches in both configs. Shell tool down → checks, P3 smoke, docker, and Electron run all pending; nothing merged, no code written. |
| 2026-09-18 | [2026-09-18.md](2026-09-18.md) | 28 | **Session 28:** the integration with the frontend/backend repo designed, no feature code written (Adarsh: code together tomorrow). The other repo turned out to be a complete running older system ("R4": Express :8080, PostgreSQL :5432, MQTT :1883, Electron + Leaflet :8085) whose Python engine our simulator **replaces** — treated as a reference, not a source of truth, since the frontend/backend team owns their side. Keystone: the selection token already exists and all three pieces agree — 10 segments of 250 m in both `app.js:270-287` and `zones.py:122-132`, and `?seg=3&day=172` already isolates a slice today, so the "3D view" button needs only a popup on their side. Dangerous: their map treats x as running *across* strike over a 600 m square while ours runs 2500 m *along* x, so raw metres would draw the mine rotated 90° and convincingly wrong — the seam passes a segment or lat/lon instead. Seven more, each measured: their marquee selection silently reports ±150 m (calls a `latLonToXY` that does not exist); port 8000 is already polled by their dashboard for `/health`, exactly where our static server sits, so we move to 8010 and answer `/health`+`/control` as a drop-in; a `file://` origin CORS-blocks fetches whatever the CSP says; their MapLibre terrain never attaches so our renderer is the only working 3D; their `readings` table has no `run_id`, no `subsidence_mm` and no `_prov` columns and its `tier` CHECK would reject 83 of our 410 nodes while loading 327 fine; the negative-down sign flip is still unimplemented so every scenario number would draw the ground rising; and L1/L2/L3/L7 have no test file — L1 being the one that proves a what-if cannot change the real run. Design: three channels that never mix, five build sittings A–E. Repo cleaned (12 `__pycache__`, 4 `.pytest_cache`, 78 `.pyc`); tracker consolidation written up and waiting on Adarsh because it touches the audit trail. vault PERFECT, mine-sim 198 passed / 1 skipped, scenario-lab 125 passed. |
| 2026-09-17 | [2026-09-17.md](2026-09-17.md) | 27  **Session 25:** git backlog cleared - all ten local branches were ancestors of one linear chain, so the unpushed half was bookkeeping; the uncommitted half (30 modified, 20 untracked, sessions 21-24) went in as five grouped commits and all 11 branches + main are now on GitHub, 0 unpushed anywhere. main NOT fast-forwarded - merging stays Adarsh's call. college-store/ (unrelated Next.js, 467 MB node_modules) ignored, not committed. New branch feat/terrain-events-scenario-lab with steps/S0-terrain-events-plan.md: eight steps S1-S8 ending at S4 where the "Freeze + create subsidence" button lights up, built around **eight live integration hazards** between F10 (17 Sep, in mine-sim) and WP9 section 6 (14 Sep) - two crack models that disagree worst at the panel corners, CrackField.update(x,y,t) unable to answer a what-if, a missing strain_xy, a silent microstrain/mm-per-m factor of 1000, and the latched crack baseline living only in out/cracks/ which a full rebuild deletes. None fails a test today because nothing joins the two halves yet. S1 makes the lab import minesim.cracks and minesim.vibration rather than reimplement them; gate L8 keeps it that way (ds = 0 -> the lab's answer must equal the simulator's, cell for cell). Five decisions D-S1..D-S5 wait on Adarsh. mine-sim 198 passed / 1 skipped, scenario-lab 42, renderer 19, vault PERFECT.| Front-end design review of Window 1 from the users' point of view (strata-control engineer, mine planner, surface officer). Six defects named with evidence: node models only visible at 9x true size; the depth ramp is an 8-stop rainbow with no shallow end, normalised to the *running* peak so a given millimetre changes colour between frames; cluster badges hide coverage holes; the isolated segment is cut 100 m deep on a 250 m block; `vertical_exaggeration` is an inert config key so scene.json says x10 while the view runs x25. The small simulated area is NOT a defect - a longwall panel is a 250x2500 m strip, so the trough is a strip. Adarsh decided all three: status dots by default, single-hue fixed 0-1300 mm ramp, and the 8-panel district switched on by **re-spacing tiers** rather than unfreezing the 1-99 anchor ID range (budget worked out: 1676 scouts must come down to <=650). Steps F7 (renderer only) and F8 (district, needs a re-run) written for Antigravity. No code written. mine-sim 157 passed / 1 skipped, vault PERFECT. **Session 21:** Adarsh chose to trim the terrain and to have Claude build both steps. DEM cut 62.2 -> 12.9 km2 (`dem_margin_m` 3000 -> 900; 400 would have clipped the gateway candidate ring and moved the gateway) with the new file bit-identical to a sub-array of the old and placement unchanged but for 7e-15 deg of lat/lon round-off. **F9** built: the floating panels became a CSS-grid shell, the hand-measured `right:346px` / `bottom:190px` offsets deleted, ten window-size reads rerouted to the viewport box, and a per-tier layer box + density control added (1B+1C off -> 60 of 375 drawn while every reported count stays put). Headless checks caught two bugs: a stale canvas CSS box and a scale bar that swallowed clicks. **F10** built: the real maths gap was the missing cross-derivative, so `curvature_xy` + `principal_strain` were added and crack bearings now rotate 40.65 deg at the panel corners; new `cracks.py` (latched opening, NCB change-of-length damage) and `vibration.py` (scaled-distance PPV, caving from face advance, DGMS limits). Cracks form a closed ring with the trough floor uncracked. Checked and REJECTED copying the old sandbox's subsidence: it is unfitted and models a different assumed mine. Stopped short of `out/nodes.csv` because contract 7.1 freezes its column order - decision pending. mine-sim 194 passed / 1 skipped / 2 xfailed (38 new tests this session, no pre-existing test changed). The 2 xfails are NOT from this work: `max_children_per_anchor` was amended 8 -> 5 in session 22, `nominal = cap-1 = 4`, so the frozen 1-99 anchor range now caps every mine at 396 scouts, `illinois_lw` (421) cannot be planned, and **F8 is arithmetically impossible as written** (its 650-scout target needs 163 anchors) - stop notice added to F8 and STATUS. renderer 19, vault PERFECT. **Session 23:** G15 repaired without touching the cap or the anchor ID range. All three exits Adarsh had been offered were refused; a fourth was found: the spare child slot per anchor is fail-over *headroom*, a preference, not a hard limit, and it was being enforced as one. A mine that cannot afford one spare slot each now spreads over all 99 anchors instead of refusing to plan, so the real per-mine ceiling is 99 x 5 = 495 scouts, not 396. `illinois_lw` plans again (421 scouts -> 99 anchors, 33 keeping a spare slot) and Adriyala is bit-identical (327 -> 82, Rs 1,029,000), so no re-run was needed. Both `xfail(strict=True)` markers removed - **invariant 8 is evidenced again**. Contract amended in four places, including two stale cap-8 leftovers (`child_index` 0..7, and a baseline line claiming 6 children per anchor against a cap of 5). F8 unblocked with its target corrected 650 -> 495 scouts - a 3.4x squeeze from 1676, so fewer than 8 panels may be the honest answer. mine-sim 198 passed / 1 skipped / 0 xfailed, vault PERFECT. **Session 24:** Two asks. (1) The inspector was showing the *planner*, not the simulator: `nodes.csv` has 21 columns per node per epoch and `export_scene.py` was exporting 5, so the panel was padded with static pre-run peaks next to static coordinates - the "fixed amount of data". Three new frame planes (telem/link/flags) carry tilt, strain, displacement, battery, RSSI, the parent actually used and the emergency-slot bit into the browser, and the inspector is regrouped into measured / world grid / planned / radio plan. Each tier now shows a different live set because each carries a different sensor package (1A tilt, 1B strain+disp, 1C strain); anchors say "relay only". Frames 37 -> 45 MB. Verified headlessly at day 400 on all four tiers. (2) The last cap on node counts is gone: `LAST_ANCHOR_ID = 99` deleted, anchors = ceil(scouts/(cap-1)) with nothing truncating it, scouts start at max(100, 1+anchors). Put back to Adarsh first because it contradicts his own "HARD CONSTRAINT, DO NOT WORK AROUND" line in F8; he confirmed. Adriyala bit-identical (82 anchors, Rs 1,029,000, node_plan.json nodes compare equal); illinois_lw unclamps 99 -> 106 and stops trading its fail-over headroom. `max_children_per_anchor` kept at 5 - it is one Anchor's radio capacity and the divisor in that count, not a ceiling. Contract 3 and 6 amended, G15 assertion 4 rewritten to contiguity + no ID collision. **F8's whole premise is gone** (it existed only to squeeze 1676 scouts under 495) - flagged with two options, not rewritten, needs Adarsh's call. mine-sim 198 passed / 1 skipped (baseline unchanged), stress test 60/0/0, renderer 19, vault PERFECT. **Session 26:** P1 of the Scenario Lab built - zones 1-10 plus full, and one crack model shared with the simulator. The zone control was a maths problem, not an if: a boolean mask leaves a cliff at the zone edge and everything downstream differentiates the surface, so it would have drawn a tidy rectangle of cracking around whichever zone was picked (measured: true rim strain 5.28 mm/m, boolean mask 95.95 - 18x the real value, 32x the cracking threshold). A zone is now a raised-cosine weight and the tapered version measures 5.28, i.e. invisible. strain_xy + principal_from_fields give the lab a strain tensor from a SURFACE (which no analytic t produces); lab/cracks.py is a bridge to minesim.cracks with the single microstrain<->mm/m conversion; gate L8 (10 assertions) ties the halves together, including a cell-for-cell comparison against export_cracks.py on its own 10 m grid - median difference 0.58%, cracked/not-cracked differing on 3 cells of 25,564. Gate L4 caught three magic numbers in the new tool; the code was fixed, not the gate. scenario-lab 70 passed (was 42), commit 3580475. **Session 27:** P2 built - consequence.py, the one evaluate() every scenario and later every forecast goes through. Cracks come back as LINE SEGMENTS with a bearing (principal-strain direction turned a quarter turn, because a fissure opens across the pull); the illustrative village (8 houses, 2 roads, 8 poles, 1 tower) is declared in the panel frame and transformed once at load, and an object off the grid is an error rather than clamped; the shaking layer is caving-only, drawn as contour lines, with blast still deferred so nothing reads the OPEN-VERIFY blast constants. The crack event carries today's sinking rate forward N days, capped by the ground's remaining capacity, and refuses with its own numbers when the ground is not moving. S7 done and WHATS-LEFT 1.5 closed: a rebuild used to delete the latched crack history and never recreate it, so run-simulation.sh gained step 9/9, cell = 10.0 became cracks.export_cell_m, and four days are exported in one shared walk of the latch (2.5 s) because the lab can freeze any day; with no baseline for that day the lab refuses to count new cracks instead of guessing. Gate L8 extended: L8-6 null scenario (ds = 0 reproduces the exported field - widths median 1.37% apart, NCB grade differs on 0.14% of 25,564 cells, 0 new cracks) and L8-7 crack bearings (median 0.00 deg from perpendicular over 4,000 lines), mutation-checked. Two defects in the new code caught by the existing gates: evaluate was laundering an int array into floats and so defeating gate L5, and a crack bearing could leave the wire as exactly 180 deg after rounding. scenario-lab 125 passed, mine-sim 198 passed / 1 skipped, renderer 19, vault PERFECT.|
| 2026-09-16 | [2026-09-16.md](2026-09-16.md) | 19 | Claude built F1–F5 on per-step branches merged into `fix-the-system`: no wires, 9 km DEM + 8 zoom levels + icon LOD, placement v3 over the whole panel (375 nodes), 690-day run on the planned network (5.4 M rows), daily binary frames, clock 1×–10000×. Session 17: real-vs-fake answered with evidence, deformation verified, thin-shell ground + 360° camera + "+/− only" zoom + graphics pass; mine district STOPPED at 1 panel (frozen 1–99 anchor ID range) and a silent fit-corruption bug fixed. mine-sim 157 · renderer 19. Session 18: Window 1 readability — solved CIE L* height ramp restored from the old sandbox, lighting lifted, relief opens at ×2 (×3/×4 added), isolated 250 m segment cut to a 100 m slab with framed camera, only that segment's nodes drawn, icons forced on while zoomed; visuals screenshotted and verified for the first time (headless Chromium + ANGLE/Metal), play-through 0 console errors. Session 19: darker deep-green ground (and the sRGB-as-linear vertex-colour bug that was gamma-brightening every ramp), node models 9× standing on the highest ground under their footprint, context terrain trimmed to 3.6×1.3 km, isolated segments turned into a thin crust with the strata slab moved behind X-ray, icons on at every zoom; placement density verified correct-by-design (28 nodes per 250 m block) but a real defect found — `site_anchors` ignores `min_node_spacing_m`, 32 anchor↔scout pairs under 20 m, left open for Adarsh. mine-sim 157 · renderer 19. |
| 2026-09-15 | [2026-09-15.md](2026-09-15.md) | 10–15 | F1 written + checked (7 fixes); branch `fix-the-system` plan F0–F5 (wires, zoom, node designs, placement spread, daily data, speeds); `./run-simulation.sh` one-command rebuild (renamed from run-all.sh, step-6 stop fixed); D1–D7 yes; DEC-16 no database; Window 1 rebuilt on real SRTM terrain; node planner v2 over the terrain (237 scouts, 34 anchors, ₹5.71 lakh). 138 tests. |
| 2026-09-14 | [2026-09-14.md](2026-09-14.md) | 02–09 | Step 0 review; plan reset; Jobs 1–3; cleanup; Claude built M1–M5 (121 tests); stress test + NaN fix; W1–W4 fixed (peak −5% vs field, 9% pinned), real-anchored dataset, CI; status check (v1 left: T1–T4 + demo). 129 tests. |
| 2026-09-13 | [2026-09-13.md](2026-09-13.md) | 01 | Audit of repo + vault, 3-day sprint plan, DEC-1…DEC-4. 15 tests. |
