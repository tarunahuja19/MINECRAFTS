# F2 — Real-size terrain, + / − zoom to fixed levels, bigger terrain

**In plain words:** Show the ground at its true size in metres and make the terrain bigger. You zoom with **+** and **−** through fixed levels (like a map), with a scale bar. Far away, nodes are small icons that never overlap. Close up, they are the 3D models from F1.

**Plan:** [F0](F0-fix-the-system-plan.md) findings 8, 9 · decision DF4.

## 1 · Paste into Antigravity

```text
Before you start, read AGENTS.md, files/11-interface-contracts-v1.md, sih-26-finale-brain/RULES.md, WP8-consequence-renderer.md §3, steps/F0-fix-the-system-plan.md and this step. Branch fix-the-system, do not commit. Only edit the files listed. If the spec can't be implemented as written, STOP and tell me why. No magic numbers: zoom levels, icon pixel sizes, distances go in renderer/config.yaml with "# source: display choice". Terrain is context only: never feeds S(x,y,t), never writes Z. Python: /opt/miniconda3/envs/pinn-sandbox/bin/python3.11. Report exact pytest counts.

STEP F2 · True scale, fixed zoom levels, level of detail, bigger terrain

Files you may edit: renderer/js/app.js, renderer/js/network.js, renderer/js/terrain.js, renderer/index.html, renderer/config.yaml, renderer/export_scene.py, renderer/tests/test_export_scene.py, mine-sim/config/assumptions.yaml (placement.dem_margin_m ONLY), mine-sim/data/real/adriyala_lw1_dem.npz (regenerated by the existing script), walkthroughs/step-f2-zoom-scale/*.

A. Bigger terrain
1. In mine-sim/config/assumptions.yaml set placement.dem_margin_m: 3000.0 (keep the comment and add "raised 15 Sep for a wider view, F2"). Rerun the existing mine-sim/scripts/fetch_dem.py (read it first; don't change its logic). Report the new DEM shape, cell, x/y extent in panel coordinates, elevation range and fetched_utc. If the fetch fails (no internet), STOP and report.
2. renderer/config.yaml context_margin_m: draw the whole DEM (replace with a key terrain_extent: full | margin plus context_margin_m, default full). Terrain mesh: keep a single draw call; if the vertex count passes 1.5 M, downsample the context area only (not the world-grid area) and say by how much.
3. Horizontal scale stays 1 scene unit = 1 m on x and y. Only vertical has exaggeration (the existing sag and relief sliders).

B. Zoom
1. renderer/config.yaml zoom.levels_view_width_m: [8000, 4000, 2000, 1000, 500, 250, 120, 60]. zoom.default_level: 2.
2. Buttons "+" and "−" at the top right of the viewport, and keyboard + / − (also = and _). Each press flies (existing fly animation) to the next level: camera distance chosen so the visible ground width at the target equals the level width. Mouse wheel: steps one level per notch (snaps, not smooth). OrbitControls min/max distance = the smallest/largest level.
3. The zoom centre is the selected node if one is selected, otherwise the ground point under the screen centre. Clamp the target inside the terrain extent.
4. Show a scale bar (bottom left of viewport, nice round length such as 50/100/250/500/1000 m, updated on every camera change) and a "view ≈ N m wide · zoom L/8" chip. Add a north/advance arrow.
5. The camera presets (Overview, Face, Section, Network, Underground, Top) snap to the nearest zoom level.

C. Level of detail for nodes (fixes "nodes too close")
1. Far (view width > zoom.icon_above_view_m, default 1000): draw every node as a screen-space icon of fixed pixel size (config zoom.icon_px, default 10): the tier shape from F1, filled with the tier colour, outlined with the live sink colour. Use THREE.Points with a per-tier sprite (canvas-generated texture, no external image) or instanced sprites. Hide the 3D models.
2. Near (view width <= zoom.models_below_view_m, default 250): 3D models at true size, icons hidden.
3. In between: icons plus small models; icons fade out as you zoom in.
4. Picking works at every level (icons use a pixel-radius test).
5. If icons overlap on screen (two icons within icon_px), draw a count badge for the cluster instead ("12"). Click on a badge zooms in one level on it.

Tests: exported scene.json terrain extent equals the new DEM extent; the world-grid area in scene.json is unchanged (array_equal with the previous export on a small run); config has the zoom keys; no minesim.physics in renderer/.

Screenshots at day 345: zoom level 0 (whole terrain, icons, cluster badges), level 3, level 5, level 7 on a 1C node (true-size model). Report FPS in Chrome at level 0 (window.__mine can expose it).

Report: new DEM facts, mine-sim pytest (138; placement tests may shift numbers because the DEM grew. List any failing test by name and STOP rather than editing mine-sim tests), renderer pytest count, scene.json size, screenshots. Write the walkthrough. STOP.
```

## 2 · Paste into Claude (check)

```text
Check step F2
```

Claude checks: DEM refetch provenance; world grid untouched; one metre = one unit horizontally; + / − steps through exactly the configured levels; icons don't overlap at level 0; tests.

## 3 · You check by hand

| # | Do | You should see |
|---|---|---|
| 1 | Press − until it stops | The whole 8 km terrain; the panel is a thin strip; nodes are small icons or number badges |
| 2 | Press + one step at a time | Scale bar changes 1000 → 500 → 250 …; "zoom L/8" counts up |
| 3 | Click a node, then press + several times | The camera zooms onto that node; at the last level you see its real-size model |
| 4 | Scroll the wheel | Moves exactly one level per notch |

## 4 · Fix prompt (only if Claude's check found problems)

_Empty until Claude's check._
