# simulation_making — Session A

Session A of an interactive mine subsidence sandbox: a headless physics core
(terrain and surface subsidence, pure NumPy/SciPy, no UI, no server). This
directory is an isolated sandbox tree, separate from the rest of the `sih26`
project.

## Getting started

```
uv sync
.venv/bin/python -m pytest
```
